"use strict";

var domani = require('../lib/domani');


describe("#Creating an event with 'on'", function() {
	it("returns a function to cancel the evt binding", function() {
		var rta = 1;

		var d = document.createElement('div');

		expect(typeof d).toBe(0);
	});
});
